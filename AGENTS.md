# AGENTS.md

## 1. 適用範囲

このファイルの指示は、このリポジトリ全体に適用する。

現在のタスクについてユーザーから明示的な指示がある場合は、その指示を本ファイルより優先する。

このファイルは恒久的な作業ルールを定義する。現在の進捗、次にやること、未完了タスクはREADMEと開発ロードマップへ分離する。

## 2. 基本方針

このリポジトリでは、正確性を維持しながら、変更範囲、調査範囲、テスト範囲、GitHub操作を必要最小限に抑える。

* 指示された目的に直接関係する変更だけを行う
* 既存の設計、命名、ディレクトリ構成、コーディング規約に従う
* 差分を小さく、目的を明確に保つ
* 目的と無関係な修正、整理、リファクタリングを行わない
* 将来必要になる可能性だけを理由に抽象化や機能追加を行わない
* タスク判断に必要なファイルから読む
* 理由なくリポジトリ全体を繰り返し調査しない
* 一度確認した内容を理由なく再調査しない
* 軽微な変更で補助担当の別Codexタスクを使用しない
* 実装、テスト、GitHub操作を必要以上に繰り返さない

### 2.1 メイン・井上・バム・中谷の4タスク固定運用

このprojectで継続利用するCodexタスクは、次の4つだけとする。井上、バム、中谷はメインタスク内のサブエージェントとして起動せず、Codexアプリ上で各担当につき1つだけ用意した固定の独立タスクを再利用する。メインエージェントは対象projectのlocal環境を指定し、同じ作業treeを読み取れる状態で依頼する。

| 固定タスク名 | 担当 |
|---|---|
| `メイン` | ユーザーとのやり取り、企画、要件定義、文書、実装、修正、最終判断 |
| `中谷（テスト担当）` | メインが未実行の対象限定テスト |
| `井上（実装前後レビュー）` | 技術方針と実装差分の厳格レビュー |
| `バム（ゲーム体験、シナリオレビュー）` | ゲーム体験、物語、シナリオのレビュー |

* 通常のレビューやテストのたびに、井上、バム、中谷の新しいタスク、fork、worktreeを作成しない
* 各担当への追加依頼は、上表と完全一致する名称の固定タスクへfollow-up messageを送り、同じタスクを継続して使用する
* 固定タスクがarchive済みの場合はunarchiveして再利用し、履歴の読込失敗などで再利用不能な場合も、ユーザーの承認なしに代替タスクを新規作成しない
* 固定タスクがまだ存在しない初回だけ、またはユーザーが明示的に置換を指示した場合だけ、新規タスクを1つ作成できる
* 古い重複タスクを自動的に削除・archiveせず、整理対象はユーザーが判断する
* 固定タスクの過去の依頼内容へ依存せず、毎回のfollow-upで`AGENTS.md`全文確認、今回の対象範囲、実行済み確認、禁止事項、期待する出力を改めて明示する

* 井上はGPT-5.6 Terra、思考レベル高を推奨する
* バムはGPT-5.6 Sol、思考レベル高を推奨する
* 中谷はGPT-5.6 Luna、思考レベル高を推奨する
* モデルを明示できない実行環境でも、担当名、責務、禁止事項、出力形式をpromptで固定する
* 井上とバムは読み取り専用とし、ファイル編集、テスト、Git操作を依頼しない
* 中谷は指定された対象限定テストの実行だけを行い、ファイル作成・編集、実装修正、Git操作を行わない
* 別タスクへは、この`AGENTS.md`の全文確認、対象範囲、既にメインが実行した確認、禁止事項、期待する出力を最初の依頼で明示する
* 同じlocal作業treeを共有するため、補助担当がメインと並行してファイルを変更する運用は行わない
* メインエージェントは別タスクの完了結果を読み、指摘の採否、修正、次工程を担当する
* 別タスクが調査範囲を広げて結論を返さない場合は、追加調査を止めて現在の証拠だけで結論を出すよう指示する。メインタスク内のサブエージェントへ切り替えない

## 3. 作業開始時の確認

作業開始時は必ず次を確認する。

1. `pwd`
2. `git status --short --branch`
3. 現在のブランチ
4. 直近のユーザー依頼
5. 変更対象の直接関連ファイル

作業フォルダは、ユーザーがVS Codeで開いているクローンを正とする。現在の主作業場所は `C:\Users\ユーザー\OneDrive\ドキュメント\developer-dungeon` である。

Codexの実行環境上の作業フォルダと、ユーザーがVS Codeで開いているリポジトリがズレていないか注意する。必要に応じて `git remote -v` も確認し、対象リポジトリが `yuya-dev-dev/developer-dungeon` であることを確認する。

古い作業フォルダや別クローンを編集しない。作業場所が不明な場合は、実装前に停止して確認する。

## 4. プロジェクト固有の制約

このプロジェクトは、新人エンジニアが開発技術を実践的に学ぶWeb application `developer-dungeon` である。Gitコマンドの隔離実行・状態採点基盤はGit編専用とする。次期実装対象として、同じrepositoryとSpring Boot application内へJavaクラス設計問題集を独立した境界で追加する。

Javaクラス設計問題集は、問題提示、固定の模範code表示、3状態の自己申告進捗だけを担い、利用者codeの保存・実行・自動採点・AI API連携を行わない。具体要件は`docs/requirements.md`15章、問題と技術境界は`docs/java-class-design-practice.md`を正本とする。Javaコードレビュー編、SQL編、Docker障害対応編は将来構想であり、別途ユーザーの承認を得て再評価するまで実装しない。将来編だけを理由に、`challenge_type`、汎用Runner、プラグイン機構、共通DB構造などを先回りして導入しない。

技術構成はJava / Spring Bootをアプリケーションの中心とし、DockerをGit課題の隔離実行に使用する。PostgreSQL、Docker Compose、画面方式、フロントエンド構成などの採用時期と詳細は、承認済みの要件文書とアーキテクチャ文書で定める。Git課題を隔離するためのDocker利用は、将来構想であるDocker学習編の実装を意味しない。

ユーザーが入力したGitコマンド、SQL、その他の操作を評価する機能は、通常のWeb入力より高い危険性を持つ。実装時は、アプリケーション本体、課題実行環境、永続データの境界を明確に分離する。

* ユーザー入力をホストOSやアプリケーションプロセス上で直接コマンドとして実行しない
* Git課題は、課題ごとに初期化された使い捨ての隔離環境で実行する
* 1日縦切り版でも隔離制約を省略せず、準備が間に合わない場合にホスト実行へフォールバックしない
* 任意のシェル実行を許可せず、許可するGit操作と引数を明示的に制限する
* 課題実行環境からホストのファイルシステム、Dockerソケット、秘密値へアクセスさせない
* 課題実行環境には、実行時間、CPU、メモリ、プロセス数、出力サイズの上限を設ける
* 課題実行環境の外部ネットワーク接続は原則禁止し、必要な場合だけ接続先を限定する
* 将来、別途承認を得てSQL編へ着手する場合は、SQL課題用DBとアプリケーション管理用DBを分離し、用途ごとに別のDBロールと最小権限を使用する
* 将来のSQL課題では、ステージの目的に応じて実行可能な文を制限し、タイムアウトと結果行数の上限を設ける
* 採点は可能な限りコマンド文字列ではなく、実行後のリポジトリやDBの状態を検証して行う
* 課題終了、タイムアウト、失敗時には、使い捨て環境と一時データを確実に破棄する
* テストデータ、サンプルデータ、初期データ、スクリーンショットには架空の情報だけを使用する
* 既存のアクセス制御仕様を、明示的な依頼なしに変更しない
* 新たなログイン、ユーザー管理、権限管理を、明示的な依頼なしに追加しない
* 依頼と無関係な既存機能の動作を変更しない
* 新しい本番用ライブラリや依存関係を、ユーザーの承認なしに追加または更新しない
* 大規模な設計変更は、ユーザーの承認なしに行わない
* Docker、Docker Compose、CI、クラウド、デプロイ設定の変更は慎重に扱う
* 本番環境、本番DB、本番認証情報には、ユーザーの明示許可なしに接続しない
* 本番データを直接変更する操作は原則行わない
* 環境変数、DB接続文字列、APIキー、パスワード、秘密値を会話、ログ、スクリーンショット、ドキュメントへ不用意に出さない
* デプロイ後は、必要に応じてデプロイ対象commitとGitHubの`main`最新commitが一致しているか確認する
* 画面変更ではPC表示だけでなく、必要に応じてスマートフォン表示の確認観点も提示する

## 5. Gitとブランチの安全規則

作業開始前に、現在のブランチと作業ツリーの状態を確認する。

* コード、DB、認証、クラウド設定、デプロイ挙動に影響する変更は、`main`へ直接コミットしない
* コード、DB、認証、クラウド設定、デプロイ挙動に影響する変更は、`main`へ直接pushしない
* ドキュメントだけの変更は、ユーザー判断でPRを作らず`main`へ直接反映してよい
* コード、DB、認証、クラウド設定、デプロイ挙動に影響する変更で現在のブランチが`main`の場合は、メインエージェントが変更前に作業用ブランチを作成して切り替える
* ドキュメントだけの変更で作業用ブランチを作成するか、`main`へ直接反映するかはユーザー判断に従う
* タスク用ブランチが指定されている場合は、そのブランチを使用する
* 強制pushを行わない
* コミット履歴を書き換えない
* ユーザーや別のエージェントが行った可能性のある変更を破棄しない
* `git reset --hard`などの破壊的なコマンドを使用しない
* 現在のタスクに関係するファイルだけをステージング対象として扱う
* PDF、スクリーンショット、一時ファイル、ローカル生成物は、ユーザーが明示しない限り`git add`しない

1つのPRには、同じ目的に属する変更だけを含める。同じ機能に関連する複数の軽微な変更は、1つの作業ブランチと1つのPRへまとめてよい。目的の異なる変更は混在させない。

## 6. 実装前の確認

実装前には、必要最小限の範囲だけを調査する。

1. ユーザーが指定したファイルを確認する
2. 変更対象の直接的な依存先を確認する
3. 既存の実装方法と近隣のテストを確認する
4. 必要に応じて関連ドキュメントを確認する
5. 変更予定の範囲を特定する

必要性が判明するまでは、調査範囲をリポジトリ全体へ広げない。

要件が曖昧で、実装によって動作やデータ構造が大きく変わる場合は、推測で実装せずユーザーへ確認する。

## 7. 実装方針

* 最小差分で実装する
* 既存コードや既存パターンを可能な限り再利用する
* 指示されていない機能を追加しない
* 目的と無関係な命名変更を行わない
* 目的と無関係なコード整形を行わない
* 目的と無関係なファイル移動を行わない
* テストのためだけに大規模なリファクタリングを行わない
* 同じタスク内の関連変更は、細かな編集ごとに中断せずまとめて実装する
* ドキュメントは、今回の変更によって記載内容が実際に変わる場合のみ更新する

### 7.1 Java実装の可読性と責務境界

今後のJava実装は、`docs/architecture.md`の責務境界と`docs/code-reading-guide.md`の読解順を維持する。

* state、同期、lifecycleを持つcoordinatorは、そのstateの唯一の所有者とする。同じstateを複数classへ分散しない
* 入力検証、変換、判定を分離する場合は、原則としてstateを持たない小さなpolicyまたはhelperとする
* 2つ目の具体的な利用箇所がない段階で、汎用interface、基底class、任意入力を受ける共通APIを先回りして作らない
* wire contract、DB schema、route、error messageなどの外部契約は、明示した変更計画なしに可読性改善へ混ぜない
* 1行に複数のfield宣言、assignment、制御構文、`return`を詰め込まず、処理順が上から追える形にする
* 変数名とtest helper名は役割または前提を表す語を使い、意味のない1文字名や過度に一般的な名前を避ける
* compatibility constructorやoverloadは、実利用、外部契約、または明確な可読性上の価値がある場合だけ残す
* test helperは重要な前提、実行順、security条件を隠さない。共通化は同じ既定準備の重複除去に限定する
* state所有者またはpackage責務を変える場合は、実装前に理由と影響を明示し、必要に応じて`docs/architecture.md`を同時更新する

実装完了後は次を確認する。

* 変更したファイルの差分
* 目的と無関係なファイルが変更されていないこと
* 依頼された機能以外へ変更が広がっていないこと
* テストコードが変更内容と一致していること

## 8. テストの役割分担

原則として、メインエージェントはテストコードを作成または修正し、実装直後に差分、コンパイル、変更箇所の最小確認を行う。文書の作成・更新、またはCSS・HTMLだけの編集を除く非軽微な実装では、井上の実装後レビューとメインエージェントによる指摘修正の後に、中谷が対象限定テストを実行する。メインと中谷は同じテストクラスまたは同じテストメソッドを重複して実行しない。中谷は、メインが未実行の変更層・実行環境・異常系のうち、今回の変更を確認する最小範囲だけを担当する。

軽微で局所的な変更では中谷の別タスクを使用せず、メインエージェントがユーザーへ正確な対象限定テストコマンドを提示し、ユーザーが実行することを基本とする。

Docker、DB、E2E、外部サービス接続、本番環境接続を伴う操作は、テスト担当にかかわらず、対象タスクについてユーザーから明示依頼がない限り実行しない。明示依頼がある場合でも、Docker Desktopまたは同等の外部実行環境が必要な作業を開始する時点で、メインエージェントはユーザーへ起動が必要であることを先に伝える。

Codexのsandbox内でMavenを実行すると、Javaの`user.home`とローカルrepositoryが`C:\Users\CodexSandboxOffline`配下へ切り替わり、実ユーザーの`.m2`に依存関係が存在しても解決前に停止する場合がある。この場合はコードまたはテストの失敗として扱わず、`-o`による同条件の再試行を繰り返さない。対象テストの実行がユーザーから許可されている場合は、メインエージェントが原因を確認したうえで、必要最小限のMavenコマンドを承認付きのsandbox外実行へ切り替える。中谷の別タスクへ依頼する場合も、この実行条件を最初の指示に含める。

メインエージェントが担当する作業：

* 変更に必要なテストコードを追加または修正する
* 既存の近隣テストを優先して利用する
* 変更内容を確認できる最小限のテストだけを追加する
* ユーザーが実行すべき正確なコマンドを提示する
* テストの種類と確認できる範囲を説明する
* DBや外部サービスへ接続する可能性がある場合は事前に警告する

中谷が担当する作業：

* 実行前にこの`AGENTS.md`、対象差分、関連するテストだけを確認する
* メインが実行済みのテストクラス、テストメソッド、同一条件のコマンドを再実行しない
* メインの実行済み範囲と未確認範囲を確認し、未確認の層または観点だけをテスト対象に選ぶ
* ファイル作成、ファイル編集、実装修正、Git操作を行わない
* 変更内容を検証できる最小のテストクラスまたはテストメソッドだけを実行する
* 実行したコマンド、結果、確認できた範囲をメインエージェントへ報告する
* 失敗時は、最初の関連失敗、主要な例外、実行コマンドだけを報告し、無関係な調査へ広げない
* メインエージェントの修正後に再実行する場合は、原則1回までとする

ユーザーが担当する作業：

* 軽微な変更で提示されたテストコマンドを実行する
* Docker、DB、E2E、外部接続を伴うテストの実行可否を判断する
* 手動で画面や操作を確認する

## 9. テスト範囲

### 軽微で局所的な変更

変更直後は、変更箇所に直接対応する対象限定テストを提示する。

対象限定テストとは、全テストを実行せず、今回変更した機能に直接関係するテストクラスまたはテストメソッドだけを実行することを指す。

テストクラスを限定する例：

```powershell
.\scripts\invoke-maven.ps1 "-Dtest=GitChallengeServiceTest" test
```

テストメソッドを限定する例：

```powershell
.\scripts\invoke-maven.ps1 "-Dtest=GitChallengeServiceTest#shouldAcceptRepositoryInExpectedState" test
```

対象限定テストに成功しても、システム全体に影響がないと断定してはならない。

### 同じ目的の実装がまとまった時点

次の場合は、Maven testフェーズの実行をユーザーへ提案する。

* 同じ目的の複数の変更が完了した
* 1つの機能が完成した
* PRを作成する直前
* 共通コードを変更した
* 複数の機能から利用される処理を変更した

Maven testフェーズの基本コマンド：

```powershell
.\scripts\invoke-maven.ps1 test
```

### より広い検証が必要な変更

以下を変更した場合は、対象限定テストだけでは不十分な可能性があるため、必要な統合テストまたは起動確認を提案する。

* DBスキーマ
* マイグレーション
* Repository
* 複数のService間の連携
* Springの設定
* 依存性注入
* ルーティング
* 共通例外処理
* 認証または認可
* 依存関係のバージョン
* Dockerまたはデプロイ設定
* 複数モジュールにまたがる変更

`verify`などのコマンドを提示する前に、`pom.xml`を確認し、そのコマンドで実際にどのテストが実行されるかを確認する。

設定を確認せず、すべての変更に対して一律に`clean test`や`clean verify`を提案しない。

### 手動確認

画面またはユーザー操作に関係する変更では、必要最小限の手動確認手順も提示する。

例：

1. アプリケーションを起動する
2. 変更した画面を開く
3. 正常な入力で操作する
4. 必要に応じて不正な入力も確認する
5. 保存結果または表示結果を確認する

## 10. 実装完了後の停止条件

実装と必要なテストコードの作成が完了したら、Codexは次を報告して停止する。

軽微な変更では、以下だけでよい。

1. 変更したファイル
2. 実装内容の要約
3. 実施した確認
4. ユーザーが次に実行すべきコマンド

リスクがある変更では、必要に応じて次も報告する。

* 最初に実行すべき対象限定テスト
* Maven testフェーズが必要かどうか
* 必要な手動確認
* テストによって確認できない範囲
* 既知の注意点または仮定

Codexは基本的に差分チェックとローカル状態確認までで停止し、commit、push、PR作成、マージはユーザーが行う。ユーザーが明示的に代理実行を依頼した場合だけ、Codexがcommit、push、PR作成を行ってよい。

## 11. テスト失敗時の対応

ユーザーまたは中谷が実行したテストの失敗は、報告されたコマンドとログをもとに調査する。

調査の順序：

1. 実行されたコマンドを確認する
2. 最初に失敗した関連テストを確認する
3. 主要な例外と最初の原因箇所を確認する
4. 必要に応じて`target/surefire-reports`内の該当レポートだけを確認する
5. 今回の変更が原因かを判断する
6. 最小差分で修正する
7. 実行担当へ再実行コマンドを提示する

Codexが実装中に明らかな構文ミス、コンパイル不整合、設定不整合を発見した場合は、その場で最小差分で修正してよい。

以下を行わない。

* 最初からログ全体を読み込む
* 無関係な失敗を調査する
* 同じログを何度も読み直す
* 原因が不明なまま大規模な修正を行う
* 同じ失敗に対して無制限にテストを繰り返す

再実行は原則1回までとする。2回修正しても解決しない場合は作業を停止し、次を報告する。

* 確認した内容
* 実施した修正
* 現時点で不明な点
* 次に行うべき最小限の調査

## 12. コマンド出力とログの制限

大量の出力を会話コンテキストへ取り込まない。

* `git status`は可能な限り簡潔な形式を使用する
* 最初に`git diff --stat`で変更規模を確認する
* 関連ファイルの差分だけを読む
* 全ログではなく、エラー周辺や末尾の必要な部分だけを読む
* 不明なコマンドの大量出力をそのまま表示しない
* 必要なら完全なログをローカルファイルへ保存し、該当箇所だけを確認する
* 同じ大きな出力を繰り返し会話へ掲載しない

コマンドが5分以上終了しない場合は、理由なく待ち続けたり再実行したりせず、停止して状況を報告する。

## 13. Commit、push、PR作成、マージ

作業用ブランチの作成と切り替えはメインエージェントが行う。`git add`、commit、push、PR作成、マージはユーザーが行う。

Codexは、ブランチ作成、実装、差分確認、ローカル状態確認、ユーザーが実行すべきGitコマンド提示までを担当する。

変更内容は、ユーザーがVS Codeで開いているクローン上で確認し、ユーザーがGitHubの`main`へ反映する。Codex側の古い作業フォルダだけに変更を残さない。

メインエージェントは、ユーザーが現在のタスクについて明示的に役割分担を変更しない限り、`git add`、commit、push、PR作成、マージを代理実行しない。

ドキュメントだけの変更は、ユーザー判断でPRを作らず`main`へ直接commit/pushしてよい。コード、DB、認証、クラウド設定、デプロイ挙動に影響する変更はPRを作成する。

基本手順：

1. メインエージェントが現在のブランチを確認し、必要な作業ブランチを作成して切り替える
2. 差分を確認する
3. ユーザーがタスクに関係するファイルだけをステージングする
4. ユーザーが変更内容を具体的に表すコミットを作成する
5. ユーザーが作業ブランチをpushする
6. ユーザーが`main`向けのPRを作成する
7. ユーザーがPRを確認してマージする

PR本文を作る場合は簡潔にし、次だけを記載する。

* 変更目的
* 主な変更内容
* 実行した、またはユーザーが実行すべきテスト
* テスト結果
* 必要な注意点

明示依頼がない限り、PR作成後に次を行わない。

* CIの完了待ち
* CIの定期的な確認
* CIログの分析
* 追加のAIコードレビュー
* 不要なコメントやレビュー依頼
* 自動マージ
* 手動マージ

## 14. 井上による厳格レビュー

別Codexタスクのレビュー担当「井上」は、企画・要件定義・全体設計に関する文書一式の最終レビューと、文書の作成・更新またはCSS・HTMLだけの編集を除く非軽微な実装のレビューに使用する。井上は実装担当でも文書作成担当でもない。

企画・要件定義・全体設計のレビューでは、関連文書を1ファイルずつレビューさせず、メインエージェントが文書一式を作成して横断的な整合性確認を終えた後に、井上が全体をまとめてレビューする。

実装前レビューは、対象範囲を絞った厳格な方針レビューとする。Runner、DB、認証、Docker、デプロイ、Java、JavaScript、設定ファイル、または複数ファイルへ影響する機能では、実装前に責務、境界、脅威、スコープ、テスト観点を確認する。

実装後レビューでは、実際の差分とテストコードを対象に、既存仕様との不整合、任意実行、権限昇格、ホスト・Dockerソケット・管理DB・秘密値へのアクセス、データ破損、テスト不足、不要な差分を確認する。

井上を使用する場合の原則：

* 通常の文言修正、進捗更新、リンク修正などの文書更新では使用しない
* プロダクト企画、要件定義、ゲーム設計、ステージ仕様、脅威モデル、アーキテクチャ、縦切り仕様、テスト戦略、ロードマップなど、相互に依存する方針文書を新規作成または大幅更新する場合は、全関連文書の完成後にまとめて使用する
* CSS・HTMLだけの編集では、変更規模にかかわらず使用しない
* その他の軽微な変更では使用しない
* 使用前にレビュー目的と対象範囲を明確にする
* 2.1に従い、固定の`井上（実装前後レビュー）`タスクを再利用し、新しい井上タスクを作成しない
* 井上にはこの`AGENTS.md`を全文読ませる
* 企画・要件定義レビューでは関連文書一式、実装前レビューでは実装方針と直接関連するファイル、実装後レビューでは対象差分と関連テストを読ませる
* 井上に実装、ファイル編集、テスト実行、ブランチ作成、commit、push、PR作成、マージを依頼しない
* 井上に本番環境、本番DB、本番認証情報へ接続させない
* 井上に秘密値、環境変数、DB接続文字列、実在する個人情報を渡さない
* 井上の指摘はメインエージェントが採用前に妥当性を判断する
* 採用しない指摘がある場合は、その理由を簡潔にユーザーへ説明する
* Verdictが`BLOCK`の場合は、問題を解消するまで文書の承認、実装、または次工程へ進まない

井上の出力形式：

* Findingsは`P1`、`P2`、`P3`の順とし、該当する重大度だけを出力する
* 各Findingには、対象、問題、根拠、最小修正案、確認観点を含める
* `P1`は次工程へ進む前に修正必須の問題とする
* `P2`は今回修正すべき問題、または採用しない理由の説明が必要な問題とする
* `P3`は今回対応しなくてもよい改善とする
* 最後にQuestionsと、`BLOCK`、`CONDITIONAL`、`PASS`のいずれかのVerdictを出力する
* 問題がない場合だけ「重大な指摘なし」と明記する

## 15. 禁止事項

明示的な依頼がない限り、以下を行わない。

* リポジトリ全体の大規模なリファクタリング
* 目的と無関係な依存関係の追加または更新
* 目的と無関係なファイルの編集
* コードスタイルの一括変更
* ドキュメントの全面書き換え
* 不要なIssueの作成または更新
* 軽微な変更ごとの全テスト実行
* 軽微な変更ごとの統合テストまたはE2Eテスト
* カバレッジ測定
* パフォーマンス測定
* 同じコマンドの不必要な再実行
* PR作成後の継続的な監視
* 依頼範囲と無関係なセキュリティ機能の追加

## 16. 依頼範囲外の重大な問題

依頼範囲外に、明らかなバグ、情報漏えい、データ破損、重大なセキュリティ問題を発見した場合は、勝手に修正しない。

次を簡潔に報告し、ユーザーの承認を待つ。

* 問題がある場所
* 想定される影響
* 今回の変更との関係
* 最小限の修正案

## 17. 非軽微な実装の標準工程

文書の作成・更新、またはCSS・HTMLだけの編集を除く、非軽微なコード変更、Runner、DB、認証、Docker、デプロイ、Java、JavaScript、設定ファイル、または複数ファイルへ影響する実装では、原則として次の順序で進める。

1. メインエージェントが実装方針と変更範囲を整理する
2. 井上が実装前の厳格レビューを行う
3. メインエージェントが指摘を判断し、必要な方針修正を行う
4. メインエージェントが実装と必要なテストコードの作成を行う
5. メインエージェントが差分、コンパイル、変更箇所の最小確認を行う
6. 井上が実装後の差分レビューを行う
7. メインエージェントが指摘を判断し、必要な修正を行う
8. 中谷が変更範囲に対応する対象限定テストを実行する
9. メインエージェントが最終差分とローカル状態を確認し、ユーザーへ報告する

テストが失敗した場合はセクション11に従い、メインエージェントが最小修正を行い、中谷が対象限定テストを原則1回だけ再実行する。レビューの詳細はセクション14、テストの詳細はセクション8、9、11、完了報告はセクション10に従う。

通常の文書更新、CSS・HTMLだけの編集、その他の影響が明確に限定された軽微な変更では、井上と中谷を使用しない。メインエージェントとユーザーの判断、メインエージェントの差分確認、必要に応じたユーザーへのテストコマンド提示だけで完了してよい。企画・要件定義・全体設計に関する文書一式はセクション18に従う。

## 18. 企画・要件定義文書の標準工程

プロダクト企画、要件定義、ゲーム設計、ステージ仕様、脅威モデル、アーキテクチャ、縦切り仕様、テスト戦略、ロードマップ、READMEなど、相互に依存する方針文書をまとめて作成または大幅更新する場合は、原則として次の順序で進める。

1. メインエージェントとユーザーが、確定事項、仮定、未確定事項を整理する
2. メインエージェントが上流文書から依存順に文書一式を作成する
3. メインエージェントが用語、要件、スコープ、セキュリティ、文書間リンクの整合性を横断確認する
4. 新規または大幅更新した物語、シナリオ、人物関係、ゲーム体験がある場合だけ、バムが該当部分をレビューする
5. メインエージェントがバムの指摘を判断し、必要な物語上の修正を行う
6. 井上が完成した関連文書一式をまとめて技術面から厳格レビューする
7. メインエージェントが井上の指摘の妥当性を判断し、必要な修正を行う
8. メインエージェントが最終差分を確認し、ユーザーへ文書一式と未確定事項を報告する

文書ごとの途中レビューには井上を使用しない。バムは物語・シナリオ部分が完成した時点で1回にまとめて使用し、技術レビューを担当しない。中谷は企画・要件定義文書の作成、レビュー、確認には使用しない。井上のVerdictが`BLOCK`で、修正が複数文書の方針へ影響した場合は、修正後の文書一式を井上が再確認してからユーザーへ最終報告する。

## 19. バムによるシナリオレビュー

別Codexタスクのレビュー担当「バム」は、物語、シナリオ、人物関係、主人公の成長、ゲームとしての面白さに限定したシナリオ作家レビューに使用する。推奨モデルはGPT-5.6 Sol、思考レベルは高とする。実行環境でモデルを明示選択できない場合でも、同じ役割とレビュー基準をpromptで固定する。

バムを使用する対象：

* 新しいステージのシナリオを作成した場合
* 既存ステージの導入、障害内容、登場人物の反応、成長beatを大幅に変更した場合
* シーズン全体の物語接続や世界観を見直す場合

通常の文言修正、進捗更新、技術仕様だけの変更、実装差分、CSS・HTMLだけの変更では使用しない。

バムの確認観点：

* 教育用ソフトの問題文ではなく、現場で誰が困り、何を守る必要があるかが伝わり、プレイヤーが新人エンジニアとして世界へ没入できるか
* プレイヤーがログや状態から仮説を立てる余地があり、導入文が正解commandを先に教えていないか
* Git操作、物語上の目的、学習目標が自然につながり、操作の暗記ではなく状況判断と振り返りを通じた学習体験になっているか
* 導入、緊張、発見、選択、復旧、反応というストーリー構成が短いドラマとして成立しているか
* 前ステージと出来事や感情が重複せず、新人エンジニアとしての主人公の判断力、責任範囲、周囲からの信頼が段階的に成長しているか
* 登場人物の性格、役割、知識、主人公との関係が前後のステージで一貫し、技術知識がない人物を不自然に無能または悪役として扱っていないか
* 新しい概念、許可する操作、ヒント量、複合条件が既習内容の上へ段階的に積み上がり、難易度の上昇が物語上の成長と対応しているか
* クリア後の反応と成長beatがプレイヤーの判断によって得た成果として感じられ、次のステージへ進みたくなる達成感があるか
* 会話、説明、操作、結果表示のテンポを損なわず、再挑戦時に邪魔にならない文章量で、物語をskipしても技術目標を理解できるか
* 世界観が将来のJava、SQL、Docker編へ接続可能でも、現在のGit編だけで一区切りつくか
* 現状の長所と確定済み要件を保ちながら、没入感、面白さ、学習効果を高めるための具体的で小さな改善案を提示できるか

バムを使用する場合の原則：

* 2.1に従い、固定の`バム（ゲーム体験、シナリオレビュー）`タスクを再利用し、新しいバムタスクを作成しない
* バムにはこの`AGENTS.md`を全文読ませる
* `docs/game-design.md`、対象ステージの`docs/git-mvp-stages.md`、前後ステージの成長beatなど、物語判断に必要な最小範囲だけを読ませる
* 技術アーキテクチャ、セキュリティ、Runner contract、DB、テスト網羅性の判定は井上へ委ねる
* バムにファイル編集、実装、テスト実行、Git操作を依頼しない
* バムの提案が確定済み技術要件や安全境界を変更する場合は採用せず、代替表現を検討する
* バムの指摘はメインエージェントが妥当性を判断してから文書へ反映する

バムの出力形式：

* Findingsは`S1`、`S2`、`S3`の順とする
* `S1`はステージの目的、出来事、主人公の行動がつながらず、ゲーム体験として成立させるため修正必須の問題とする
* `S2`は今回修正すべき単調さ、説明過多、感情や成長の重複、正解誘導などの問題とする
* `S3`は今回対応しなくてもよい演出や文体の改善案とする
* 各Findingには対象、問題、プレイヤー体験への影響、最小修正案を含める
* 最後にStrengths、Questionsと、`BLOCK`、`CONDITIONAL`、`PASS`のいずれかのVerdictを出力する
